import styled from 'styled-components';

const BackArrowStyle = styled.img`
  position: fixed;
  bottom: 5vh;
  left: 10vw;
  margin-left: auto;
  margin-right: auto;
`;

const BackArrowContainer = styled.button`
position: fixed;
bottom: 5vh;
left: 10vw;
margin-left: auto;
margin-right: auto;
background: transparent;
border-color: transparent;
`;

export {BackArrowStyle, BackArrowContainer};
