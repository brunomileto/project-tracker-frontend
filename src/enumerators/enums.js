const locStorage = {
  email: "userEmail",
  token: "token",
  projectId: "projectId"
}

const requestMethods = {
  post: "post",
  get: "get"
}

const pages = {
  home: "/",
  projects: "projects",
  login: "login",
  register: "register",
  addProject: "addProject",
  tasks:"tasks",
  addTask:"addTask",
  cron:"cron"

}


export {locStorage, requestMethods, pages}